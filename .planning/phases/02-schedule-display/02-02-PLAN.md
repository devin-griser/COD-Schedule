---
phase: 02-schedule-display
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/TeamLogo.tsx
  - src/components/TeamSlot.tsx
  - src/components/MatchCard.tsx
  - src/components/SkeletonCard.tsx
  - src/components/ErrorState.tsx
autonomous: true
requirements: [SCHED-04, SCHED-06, SCHED-07, DSGN-02, DSGN-03]

must_haves:
  truths:
    - "Each match card shows both team logos and names side-by-side (home left, away right)"
    - "Team logos fall back to placeholder.png on error without infinite loop"
    - "Whole match card is a tappable anchor element opening the stream link"
    - "Tournament context (stage + round) is visible on every card"
    - "Completed match cards show scores with winner bold/bright and loser dimmed"
    - "TBD opponent renders as 'TBD' text without crashing"
    - "Match time or score is displayed in the center column"
    - "Team color accents applied via CSS custom properties (not dynamic Tailwind classes)"
    - "Skeleton loading cards animate with pulse effect"
  artifacts:
    - path: "src/components/TeamLogo.tsx"
      provides: "Team logo image with onError fallback"
      exports: ["TeamLogo"]
    - path: "src/components/TeamSlot.tsx"
      provides: "Team logo + name with color accent, handles TBD"
      exports: ["TeamSlot"]
    - path: "src/components/MatchCard.tsx"
      provides: "Full match card — tappable anchor with teams, score/time, tournament context"
      exports: ["MatchCard"]
    - path: "src/components/SkeletonCard.tsx"
      provides: "Loading placeholder card with animate-pulse"
      exports: ["SkeletonCard"]
    - path: "src/components/ErrorState.tsx"
      provides: "Error display for data load failure"
      exports: ["ErrorState"]
  key_links:
    - from: "src/components/MatchCard.tsx"
      to: "src/components/TeamSlot.tsx"
      via: "imports TeamSlot for home and away sides"
      pattern: "import.*TeamSlot"
    - from: "src/components/TeamSlot.tsx"
      to: "src/components/TeamLogo.tsx"
      via: "imports TeamLogo for logo rendering"
      pattern: "import.*TeamLogo"
    - from: "src/components/MatchCard.tsx"
      to: "src/utils/dateHelpers.ts"
      via: "imports formatMatchTime for time display"
      pattern: "import.*formatMatchTime"
    - from: "src/components/TeamSlot.tsx"
      to: "team.primaryColor"
      via: "CSS custom property --team-color in style prop"
      pattern: "--team-color"
---

<objective>
Build all reusable match display components: TeamLogo (image with fallback), TeamSlot (team name + logo with color accent), MatchCard (full match row with tappable anchor), SkeletonCard (loading state), and ErrorState (data load failure).

Purpose: Create the complete component library that Plan 02-03 will compose into the page layout. Each component is self-contained and follows the locked user decisions for layout, styling, and interaction.

Output: 5 new component files in src/components/. All match rendering logic is encapsulated — the page assembly plan only needs to compose these.
</objective>

<execution_context>
@/Users/devingriser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devingriser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-display/02-RESEARCH.md
@.planning/phases/02-schedule-display/02-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from existing code. -->

From src/types/index.ts:
```typescript
export type MatchStatus = 'scheduled' | 'live' | 'completed' | 'postponed'

export interface TeamRef {
  id: string
  name: string
  shortName: string
}

export interface Match {
  id: string
  status: MatchStatus
  scheduledAt: string           // UTC ISO 8601
  homeTeam: TeamRef | 'TBD'
  awayTeam: TeamRef | 'TBD'
  scores?: { home: number; away: number }
  streamUrl: string
  tournament: { name: string; stage: string; round: string }
}

export interface Team {
  id: string
  fullName: string
  shortName: string
  city: string
  primaryColor: string   // hex, e.g. "#4CAF50"
  secondaryColor: string
  logoPath: string       // "/teams/optic-texas.png"
  logoFallback: string   // "/teams/placeholder.png"
}
```

From src/utils/dateHelpers.ts (created in Plan 02-01):
```typescript
export function formatMatchTime(utcString: string, timeZone: string): string
export function formatDateLabel(utcString: string, timeZone: string): string
export function getDateKey(utcString: string, timeZone: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TeamLogo and TeamSlot components</name>
  <files>src/components/TeamLogo.tsx, src/components/TeamSlot.tsx</files>
  <action>
Create 2 component files:

**src/components/TeamLogo.tsx:**
- Props: `{ team: Team; size?: number }` (default size 32)
- Use `useState` to track `src` (initialized to `team.logoPath`) and `errored` boolean (initialized to false)
- Render `<img>` with `src`, `alt={team.shortName}`, `width={size}`, `height={size}`, `className="rounded-sm object-contain"`
- `onError` handler: if `!errored`, set `errored = true` and `setSrc(team.logoFallback)`. This prevents infinite loop if fallback also fails.
- Import `type Team` from `../types/index.ts` (use `import type` for type-only import per verbatimModuleSyntax)

**src/components/TeamSlot.tsx:**
- Props: `{ team: Team | null; matchTeam: TeamRef | 'TBD'; side?: 'home' | 'away'; dimmed?: boolean }`
- Default `side` to `'home'`, default `dimmed` to `false`
- **TBD handling:** If `matchTeam === 'TBD'` (string check), render a simple `<div>` with "TBD" text in gray-500, no logo, no color accent. Use `flex items-center gap-2` layout matching the team layout.
- **Team rendering:** If `team` is not null:
  - Outer `<div>` with `flex items-center gap-2`
  - If `side === 'away'`, reverse the flex direction with `flex-row-reverse` so logo appears on the right
  - Apply team color accent via CSS custom property: `style={{ '--team-color': team.primaryColor } as React.CSSProperties}`
  - Add a thin left border (or right for away side) using `border-l-2 border-(--team-color) pl-2` (home) or `border-r-2 border-(--team-color) pr-2` (away)
  - Render `<TeamLogo team={team} size={32} />`
  - Render `<span className="text-sm font-medium text-white">{team.shortName}</span>`
  - If `dimmed` is true, add `opacity-50` to the outer div
- **Null team (no match in teamMap):** Render the matchTeam shortName (if TeamRef) in gray-400, no logo. Defensive fallback.
- Ensure the component fills its flex space: add `flex-1` to the outer div and appropriate `justify-end` for away side.

**CRITICAL — Dynamic color anti-pattern:** NEVER write `border-[${team.primaryColor}]` — this class is purged at build time. ALWAYS use the CSS custom property approach: `style={{ '--team-color': color }}` + `className="border-(--team-color)"`. This is the canonical Tailwind v4 pattern per RESEARCH.md Pattern 2.
  </action>
  <verify>
    <automated>cd /Users/devingriser/Desktop/COD-Schedule && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TeamLogo renders img with onError fallback and errored guard. TeamSlot renders team logo + shortName with dynamic color accent via CSS custom property. TBD opponent renders "TBD" text. Away side reverses layout. Dimmed teams show opacity-50. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create MatchCard, SkeletonCard, and ErrorState components</name>
  <files>src/components/MatchCard.tsx, src/components/SkeletonCard.tsx, src/components/ErrorState.tsx</files>
  <action>
Create 3 component files:

**src/components/MatchCard.tsx:**
- Props: `{ match: Match; teamMap: Map<string, Team>; timeZone: string }`
- **Team lookup:** Resolve home and away teams from `teamMap`:
  ```
  const homeTeam = typeof match.homeTeam !== 'string' ? teamMap.get(match.homeTeam.id) ?? null : null
  const awayTeam = typeof match.awayTeam !== 'string' ? teamMap.get(match.awayTeam.id) ?? null : null
  ```
  Guard against `'TBD'` string — strings don't have `.id` (Pitfall 5 from RESEARCH.md).
- **Winner detection:** Only for completed matches with scores:
  ```
  const isCompleted = match.status === 'completed'
  const homeWins = isCompleted && match.scores ? match.scores.home > match.scores.away : null
  ```
  Use optional chaining on `match.scores` (Pitfall 4 from RESEARCH.md).
- **Card structure — the entire card is an `<a>` element (LOCKED DECISION):**
  - `<a href={match.streamUrl} target="_blank" rel="noopener noreferrer" className="block ...">`
  - Styling: `border border-gray-800 rounded-lg px-4 py-3 hover:border-gray-600 transition-colors`
  - Flat card with subtle border — no shadows (LOCKED: "no shadows, minimal and dense")
- **Tournament context row:** At the top of the card:
  - `<div className="text-xs text-gray-500 mb-2">{match.tournament.stage} · {match.tournament.round}</div>`
- **Teams + score/time row:** Flex layout with 3 columns:
  - Left: `<TeamSlot team={homeTeam} matchTeam={match.homeTeam} side="home" dimmed={homeWins === false} />`
  - Center: Score (completed) or time (scheduled/live)
    - **Completed:** Show `{match.scores?.home ?? 0} - {match.scores?.away ?? 0}` in `text-lg font-bold text-white`
    - **Scheduled:** Show `formatMatchTime(match.scheduledAt, timeZone)` in `text-sm text-gray-400`
    - **Live:** Show "LIVE" in `text-sm font-bold text-green-400`
  - Right: `<TeamSlot team={awayTeam} matchTeam={match.awayTeam} side="away" dimmed={homeWins === true} />`
  - Use `flex items-center` on the row. Center column: `text-center min-w-[60px]` to keep consistent width.
- Import `formatMatchTime` from `../utils/dateHelpers.ts`
- Import `TeamSlot` from `./TeamSlot.tsx`
- Import types with `import type`

**src/components/SkeletonCard.tsx:**
- No props needed.
- Render a skeleton that mimics the MatchCard layout with `animate-pulse`:
  - Outer div: same border/padding as MatchCard (`border border-gray-800 rounded-lg px-4 py-3`)
  - Top row: one small gray-800 rounded block (tournament context placeholder), width ~40%
  - Bottom row: flex with 3 columns mimicking team-score-team
    - Left: `w-8 h-8 bg-gray-800 rounded-sm` (logo) + `h-4 bg-gray-800 rounded w-16` (name)
    - Center: `h-6 bg-gray-800 rounded w-10` (score/time)
    - Right: mirror of left
  - Add `animate-pulse` to the outer div
  - Export as named export `SkeletonCard`

**src/components/ErrorState.tsx:**
- Props: `{ message?: string }` (default: "Failed to load schedule data")
- Render centered error message:
  - Outer: `flex flex-col items-center justify-center py-12 text-center`
  - Icon: Simple "!" text or SVG in a rounded circle with `text-red-400 bg-red-400/10`
  - Message: `<p className="text-gray-400 mt-3">{message}</p>`
  - Retry hint: `<p className="text-gray-600 text-sm mt-1">Try refreshing the page</p>`
  - Export as named export `ErrorState`
  </action>
  <verify>
    <automated>cd /Users/devingriser/Desktop/COD-Schedule && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MatchCard renders as tappable anchor element with tournament context, team slots with dynamic color accents, and score (completed) or time (scheduled) in center. TBD opponents handled without crash. Winner side is bright, loser side is dimmed. SkeletonCard mimics MatchCard layout with animate-pulse. ErrorState shows error message with retry hint. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors across all component files
2. `npm run build` — Vite build succeeds
3. All 5 component files exist in src/components/ with correct named exports
4. MatchCard uses `<a>` as root element (whole card tappable — locked decision)
5. TeamSlot uses CSS custom property `--team-color` for dynamic color accent (not dynamic Tailwind class)
6. TeamLogo has onError fallback with errored guard against infinite loop
7. MatchCard handles TBD opponent via typeof string check without runtime error
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- Vite build succeeds
- MatchCard is an `<a>` element wrapping all content (locked decision: whole card tappable)
- Tournament context visible on every card (locked decision)
- Completed cards show scores with winner bright / loser dimmed (locked decision)
- Team color accents use CSS custom properties, not dynamic Tailwind classes
- TBD opponent renders without crash
- SkeletonCard uses animate-pulse for loading state
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-display/02-02-SUMMARY.md`
</output>
